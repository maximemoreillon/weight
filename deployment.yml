apiVersion: apps/v1
kind: Deployment
metadata:
  name: weight
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weight
  template:
    metadata:
      labels:
        app: weight
    spec:
      containers:
      - name: weight
        image: moreillon/weight
        ports:
        - containerPort: 8633
---
apiVersion: v1
kind: Service
metadata:
  labels:
    run: weight
  name: weight
spec:
  ports:
  - port: 8633
    nodePort: 30633
  selector:
    app: weight
  type: LoadBalancer
